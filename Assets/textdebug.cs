using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class textdebug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
    //    Debug.Log("해당 씬 시작되었습니다");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
